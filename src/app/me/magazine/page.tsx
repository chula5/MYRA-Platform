import MagazineClient from './MagazineClient'

export const dynamic = 'force-dynamic'

// MYRA MAGAZINE — the newsletters she already gets, read for her.
export default function MagazinePage() {
  return <MagazineClient />
}
